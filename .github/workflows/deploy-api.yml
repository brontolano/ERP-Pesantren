name: Deploy API to VPS

on:
  push:
    branches: [main]
    paths:
      - 'asy-syifaa-api/**'
  workflow_dispatch:

concurrency:
  group: deploy-api
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Validate required secrets
        run: |
          missing=""
          [ -z "${{ secrets.HOSTINGER_HOST }}" ] && missing="$missing HOSTINGER_HOST"
          [ -z "${{ secrets.HOSTINGER_USER }}" ] && missing="$missing HOSTINGER_USER"
          [ -z "${{ secrets.HOSTINGER_SSH_KEY }}" ] && missing="$missing HOSTINGER_SSH_KEY"
          [ -z "${{ secrets.HOSTINGER_PORT }}" ] && missing="$missing HOSTINGER_PORT"
          if [ -n "$missing" ]; then
            echo "::error::Missing secrets:$missing"
            exit 1
          fi
          if [ -n "${{ secrets.HOSTINGER_SSH_PASSPHRASE }}" ]; then
            echo "==> HOSTINGER_SSH_PASSPHRASE detected (encrypted key mode)"
          else
            echo "==> HOSTINGER_SSH_PASSPHRASE empty (non-encrypted key mode)"
          fi

      - name: Deploy API via git pull on VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          passphrase: ${{ secrets.HOSTINGER_SSH_PASSPHRASE || '' }}
          port: ${{ secrets.HOSTINGER_PORT }}
          command_timeout: 8m
          script: |
            set -euo pipefail
            TARGET_DIR="/opt/asy-syifaa/backend"
            [ -d "$TARGET_DIR" ] || { echo "::error::Missing $TARGET_DIR"; exit 1; }

            cd "$TARGET_DIR"
            echo "==> Pulling latest changes..."
            git stash --include-untracked 2>/dev/null || true
            git pull origin main

            echo "==> Installing dependencies..."
            npm ci --omit=dev

            if [ -f prisma/schema.prisma ]; then
              echo "==> Running database migrations..."
              npx prisma migrate deploy
            fi

            echo "==> Restarting API via PM2..."
            pm2 restart asy-syifaa-api
            pm2 save || true
            sleep 3

            echo "==> Checking API health..."
            curl -fsS http://127.0.0.1:3100/api/health
            echo "==> Checking public website endpoints..."
            curl -fsS "http://127.0.0.1:3100/api/public/students" > /dev/null
            curl -fsS "http://127.0.0.1:3100/api/public/staff?status=aktif" > /dev/null
            curl -fsS "http://127.0.0.1:3100/api/public/content/gallery?limit=200" > /dev/null
            echo "==> API deploy complete"
